#include "main.h"
/**
 *main - entry point
 *
 *@ b - prints the binary representation of an unsigned integer
 *@ u - prints unsigned decimal integer
 *@ x - prints unsigned hexadecimal integer
 *@ o - prints an unsigned ingeger in octal base representation
 *@ X - prints unsigned decimal integer
 * return :  a non negative number
 */
int main(unsigned int, num, ...)
{
unsigned int a;
unsigned int ret_value = 0
	 for (a = 0; b[a] != '\0'; a++)
	 {
		 _putchar(b[a]);
		 ret_val++;
	 }
        for (a = 0; u[a] != '\0'; a++)
	{
		_putchar(u[a]);
		ret_val++;
	for (a = 0; x[a] != '\0'; a++)
	{ 
		_putchar(x[a]);
		ret_val++;
	}
	for (a = 0; o[a] != '\0'; a++)
	{
		_putchar(o[a]);
		ret_val++;
	for (a = 0; X[a] != '\0'; a++)
	{
		_putchar(X[a]);
		ret_val++;
	}
	return (ret_val);
}
